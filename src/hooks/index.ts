/**
 * Shared custom hooks
 * 
 * This barrel export provides access to all shared hooks.
 * Module-specific hooks should be exported from their respective modules.
 */

// Shared hooks will be exported here as they are created
// Example: export { useAuth } from './useAuth'

